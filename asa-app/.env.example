# Copy this file to .env.local and fill in your values.
# Never commit .env.local to version control.

# Google Apps Script deployment URL (from Extensions > Apps Script > Deploy > Web app)
# Leave blank to run in local/demo mode (no emails, no Sheets sync).
VITE_BACKEND_URL=
